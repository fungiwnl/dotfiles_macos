- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.vim: vim
     ~/.vimrc:
     ~/.gitconfig:
     ~/.hyper.js:

- create:
  - ~/dev

- shell:
  - [git submodule update --init --recursive, Installing submodules]
