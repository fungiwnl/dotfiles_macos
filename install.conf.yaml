- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.vim: vim
     ~/.vimrc:
     ~/.gitconfig:

- create:
  - ~/dev

- shell:
  - [git submodule update --init --recursive, Installing submodules]
